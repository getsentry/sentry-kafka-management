### Local Actions

This dir contains actions which need to be run locally on a broker.

- `filesystem.py`: Interact with the local broker's filesystem
- `kafka_cli.py`: Run and parse output from built-in Kafka CLI commands
- `server_properties.py`: Read and parse Kafka `server.properties` files
